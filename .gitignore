# Compiled files (the stuff STS/Maven creates)
target/
bin/

# IDE specific files (STS/Eclipse settings)
.settings/
.project
.classpath
.factorypath

# Maven wrapper
.mvn/
mvnw
mvnw.cmd

# --- DAY 2 SPECIAL ADDITION ---
# Private uploads (prevents your PDFs from going public)
uploads/

# --- DAY 3 SECURITY ---
# Sensitive configuration (API Keys & DB Passwords)
src/main/resources/application.properties